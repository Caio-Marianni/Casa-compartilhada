# Supabase → Settings → API. Copie este arquivo para .env
# A anon key é pública por design (a RLS é que protege). A service_role key
# NUNCA entra aqui: ela ignora RLS e este arquivo vai para o navegador.
VITE_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=
